spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/bombadle_test
    username: sa
    password: sa
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
bombadle:
  daily-reset:
    enabled: true
  jwt:
    secret: "superkluczdotestownoeloeloopukpukpukpukplakalplakalsigmasigmaboy"
server:
  port: 0
